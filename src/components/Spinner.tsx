import { Stack, CircularProgress } from "@mui/material";

const Spinner = () => {
  return (
    <Stack alignItems={"center"}>
      <CircularProgress />
    </Stack>
  );
};

export default Spinner;
